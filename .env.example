# Copy this file to .env and fill with non-sensitive, short-lived values for local dev.
# Long-lived provider API keys MUST stay on the server / edge functions.

# Supabase project base URL (no trailing slash)
SUPABASE_URL=https://your-project.supabase.co
# Functions URL usually same base; override if using custom domain
SUPABASE_FUNCTIONS_URL=https://your-project.functions.supabase.co
# Temporary session token issued during local dev (leave blank to rely on auth flow)
SUPABASE_SESSION_TOKEN=

# Ephemeral provider session tokens (issued by edge functions; leave empty locally)
OPENAI_SESSION_TOKEN=
GEMINI_SESSION_TOKEN=
ANTHROPIC_SESSION_TOKEN=
PERPLEXITY_SESSION_TOKEN=
